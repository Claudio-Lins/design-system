import { Meta } from "@storybook/addon-docs";

<Meta title="Home" />

# @claudio-lins-ui
**@claudio-lins-ui** is a library of React components that I use in my projects.
*You can install it with:* `npm install @claudio-lins-ui`

## Components